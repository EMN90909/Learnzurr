create table if not exists classroom_announcements (id uuid primary key default gen_random_uuid(), room_id uuid references classroom_rooms(id) on delete cascade, teacher_id uuid, title text not null, body text, created_at timestamptz default now()); alter table classroom_announcements enable row level security; create index if not exists idx_classroom_announcements_room on classroom_announcements(room_id);
