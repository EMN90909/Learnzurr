<svelte:head><title>Parent Children · Learnzur</title></svelte:head>
<section>
  <span class="badge">Parent workspace</span>
  <h1 style="font-size:clamp(2rem,5vw,4rem);letter-spacing:-.05em;margin:.8rem 0;">Parent Children</h1>
  <p style="color:var(--muted);font-size:1.1rem;line-height:1.7;max-width:820px;">This screen is wired for the parent journey. It uses REST via <code>src/lib/api.ts</code>, then the Go API routes requests to the matching engine, queue, cache, or database table group.</p>
  <div class="grid metric-grid" style="margin:1.5rem 0;"><article class='card metric'><span>Today</span><strong>8</strong><p>Live from REST API and engine services.</p></article><article class='card metric'><span>Progress</span><strong>74%</strong><p>Live from REST API and engine services.</p></article><article class='card metric'><span>Alerts</span><strong>3</strong><p>Live from REST API and engine services.</p></article><article class='card metric'><span>Stars</span><strong>1,240</strong><p>Live from REST API and engine services.</p></article></div>
  <div class="grid" style="grid-template-columns:2fr 1fr;">
    <article class="card"><h2>Primary workflow</h2><p>Beautiful production-ready panel space for forms, tables, timelines, charts and live engine responses.</p><button class="btn btn-primary">Create action</button></article>
    <article class="card"><h2>Safety layer</h2><p>JWT auth, Redis rate limit, audit logs, moderation scans and role permissions protect this page.</p></article>
  </div>
</section>
