export type MoneySplit = { bucket: 'teacher'|'platform'|'rewards'|'tax'; amountKES: number; description: string };
export type PaymentQuote = { grossKES: number; splits: MoneySplit[]; idempotencyKey: string; display: string };
export function formatKES(amount: number) { return new Intl.NumberFormat('en-KE', { style:'currency', currency:'KES', maximumFractionDigits:0 }).format(amount); }
export function quoteEnrollmentPayment(classId: string, learnerId: string, amountKES: number): PaymentQuote { if(amountKES <= 0) throw new Error('Amount must be positive'); const teacher=Math.round(amountKES*.82); const platform=Math.round(amountKES*.10); const rewards=Math.round(amountKES*.05); const tax=amountKES-teacher-platform-rewards; return { grossKES: amountKES, idempotencyKey:`enroll:${classId}:${learnerId}:${amountKES}`, display:formatKES(amountKES), splits:[{bucket:'teacher', amountKES:teacher, description:'Teacher earning held until class delivery rules pass'}, {bucket:'platform', amountKES:platform, description:'Platform operations and support'}, {bucket:'rewards', amountKES:rewards, description:'Learner rewards and contests reserve'}, {bucket:'tax', amountKES:tax, description:'Tax and statutory reserve'}] }; }
export function normalizeMpesaPhone(phone: string) { const cleaned = phone.replace(/[ +\-]/g,''); if(/^07\d{8}$/.test(cleaned)) return `254${cleaned.slice(1)}`; if(/^2547\d{8}$/.test(cleaned)) return cleaned; throw new Error('Use a valid Kenyan Safaricom number such as 0712345678.'); }
