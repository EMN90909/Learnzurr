"use client";

import AdminBranches from "./Branches";

const AdminCompliance = () => (
  <AdminBranches
    title="Compliance & Plans"
    description="Review the same Supabase user and provider records shown in the directory, including role, plan, status, location, and account controls."
  />
);

export default AdminCompliance;
