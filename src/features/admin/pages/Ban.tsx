import AdminBranches from "@/features/erp/pages/Branches";

export default function AdminBanPage() {
  return (
    <AdminBranches
      title="Ban, Roles, and Plan Controls"
      description="Read current user state, change role, change plan, ban, unban, or delete users."
    />
  );
}
