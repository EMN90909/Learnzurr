# Learnzur 01 Request Lifecycle

## 1. Frontend Api.Ts Constructs Request
- frontend api.ts constructs request rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- frontend api.ts constructs request rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 2. Nginx Forwards Based On Path
- nginx forwards based on path rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- nginx forwards based on path rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 3. Cmd/Api Applies Middleware
- cmd/api applies middleware rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- cmd/api applies middleware rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 4. Handler Validates Ownership
- handler validates ownership rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- handler validates ownership rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 5. Engine Service Executes Transaction
- engine service executes transaction rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- engine service executes transaction rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 6. Queue Emits Async Work
- queue emits async work rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- queue emits async work rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 7. Notify Sends Result
- notify sends result rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- notify sends result rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
