# Learnzur 05 Ui System

## 1. Aqua Token Palette
- aqua token palette rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- aqua token palette rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 2. Glass Cards
- glass cards rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- glass cards rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 3. Blob Animation
- blob animation rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- blob animation rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 4. Dashboard Shell
- dashboard shell rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- dashboard shell rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 5. Responsive Nav
- responsive nav rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- responsive nav rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 6. Loader States
- loader states rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- loader states rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 7. Empty States
- empty states rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- empty states rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
