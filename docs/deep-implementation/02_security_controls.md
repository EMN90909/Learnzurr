# Learnzur 02 Security Controls

## 1. Jwt Scope Checks
- jwt scope checks rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- jwt scope checks rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 2. Rls Tenant Separation
- rls tenant separation rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rls tenant separation rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 3. Rate Limits By Role
- rate limits by role rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- rate limits by role rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 4. Idempotency Keys
- idempotency keys rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- idempotency keys rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 5. Mpesa Callback Verification
- mpesa callback verification rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- mpesa callback verification rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 6. File Mime Checks
- file mime checks rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- file mime checks rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.

## 7. Child Chat Moderation
- child chat moderation rule 1: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 2: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 3: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 4: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 5: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 6: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 7: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 8: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 9: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 10: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 11: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 12: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 13: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 14: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 15: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 16: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 17: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 18: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 19: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 20: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 21: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 22: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
- child chat moderation rule 23: the implementation keeps the source of truth in PostgreSQL, caches only derived state in Redis, writes an audit event for sensitive actions, and returns typed errors to the SvelteKit client through the Go REST boundary.
