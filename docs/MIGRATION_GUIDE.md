# Migration Guide

Use Supabase migrations in order, then run API and worker. Move existing platform concepts into Learnzur table groups: users, classes, LMS, gamfy, payments, marketplace, media, notify and moderation.
