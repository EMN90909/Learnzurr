package mearn

// ImplementationRule describes executable business behavior that the mearn engine must preserve.
type ImplementationRule struct {
	ID      string
	Name    string
	Intent  string
	Input   string
	Process string
	Failure string
}

func ImplementationCatalog() []ImplementationRule {
	return []ImplementationRule{
		{ID: "MEARN-001", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-002", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-003", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-004", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-005", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-006", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-007", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-008", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-009", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-010", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-011", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-012", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-013", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-014", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-015", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-016", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-017", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-018", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-019", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-020", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-021", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-022", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-023", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-024", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-025", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-026", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-027", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-028", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-029", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-030", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-031", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-032", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-033", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-034", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-035", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-036", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-037", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-038", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-039", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-040", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-041", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-042", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-043", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-044", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-045", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-046", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-047", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-048", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-049", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-050", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-051", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-052", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-053", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-054", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-055", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-056", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-057", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-058", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-059", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-060", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-061", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-062", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-063", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-064", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-065", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-066", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-067", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-068", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-069", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-070", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-071", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-072", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-073", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-074", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-075", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-076", Name: "Mpesa Callback Validation", Intent: "mearn applies mpesa callback validation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for mpesa callback validation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when mpesa callback validation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-077", Name: "Split Ledger Balancing", Intent: "mearn applies split ledger balancing so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for split ledger balancing.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when split ledger balancing changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-078", Name: "Payout Minimum Enforcement", Intent: "mearn applies payout minimum enforcement so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for payout minimum enforcement.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when payout minimum enforcement changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-079", Name: "Treasury Pot Allocation", Intent: "mearn applies treasury pot allocation so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for treasury pot allocation.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when treasury pot allocation changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
		{ID: "MEARN-080", Name: "Fraud Duplicate Detection", Intent: "mearn applies fraud duplicate detection so that Learnzur keeps one source of truth across SvelteKit, the Go API, Redis Streams and PostgreSQL.", Input: "Validated request context, actor role, resource identifier, idempotency key and audit metadata for fraud duplicate detection.", Process: "Normalize inputs, check ownership, read cached state, verify database state, write an append-only audit event, then emit Redis Stream jobs for downstream engines when fraud duplicate detection changes user-visible state.", Failure: "On failure the engine returns a typed error, keeps the previous committed state, records audit severity, and schedules notification only when user action is required."},
	}
}
